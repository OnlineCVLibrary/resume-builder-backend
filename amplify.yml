version: 0.2
phases:
  pre_build:
    commands:
      - echo "20" > .nvmrc
      - nvm install 20
  build:
    commands:
      - npm install
      - amplify push --yes
cache:
  paths:
    - 'node_modules/**/*'
    - 'amplify/.amplify/artifacts/cache/**/*'